<UserControl x:Class="CompanyManagementSystem.Presentation.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
        <TextBlock Text="Login" Style="{StaticResource HeadingText}" TextAlignment="Center" />

        <TextBlock Text="Username" Style="{StaticResource FormLabel}" />
        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Padding="4" />

        <TextBlock Text="Password" Style="{StaticResource FormLabel}" />
        <PasswordBox x:Name="PasswordInput" Margin="0,0,0,20" Padding="4" />

        <Button Content="Login" Command="{Binding LoginCommand}"
                CommandParameter="{Binding ElementName=PasswordInput}" 
                Padding="10,8" Margin="0,0,0,8" />
        <Button Content="Back" Command="{Binding CancelCommand}" Padding="10,8" />
    </StackPanel>
</UserControl>
