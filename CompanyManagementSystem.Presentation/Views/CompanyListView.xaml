<UserControl x:Class="CompanyManagementSystem.Presentation.Views.CompanyListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <!-- Main Card Container -->
        <Border Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Select Company" Style="{StaticResource HeaderStyle}" />
                    <TextBlock Text="Choose an existing company to manage" Style="{StaticResource SubHeaderStyle}" Margin="0" />
                </StackPanel>

                <!-- List -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding Companies}" 
                          SelectedItem="{Binding SelectedCompany}" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" 
                          SelectionMode="Single"
                          SelectionUnit="FullRow">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Company Name" Binding="{Binding CompanyName}" Width="*" />
                        <DataGridTextColumn Header="GSTIN" Binding="{Binding GSTIN}" Width="150" />
                        <DataGridTextColumn Header="Country" Binding="{Binding Country}" Width="150" />
                        <DataGridTextColumn Header="State" Binding="{Binding State}" Width="150" />
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Actions -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="Back" Command="{Binding BackCommand}" 
                            Style="{StaticResource SecondaryBtn}" Margin="0,0,10,0" Width="100"/>
                    <Button Content="Open &amp; Login" Command="{Binding OpenCommand}" 
                            Style="{StaticResource ModernBtn}" Width="140"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
