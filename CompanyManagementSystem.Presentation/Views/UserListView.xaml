<UserControl x:Class="CompanyManagementSystem.Presentation.Views.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="20">
        <TextBlock Text="User List" Style="{StaticResource HeadingText}" DockPanel.Dock="Top" />

        <Button Content="Back to Dashboard" Command="{Binding BackCommand}" 
                DockPanel.Dock="Bottom" HorizontalAlignment="Left" Padding="16,6" Margin="0,10,0,0" />

        <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="60" />
                <DataGridTextColumn Header="Username" Binding="{Binding UserName}" Width="*" />
                <DataGridTextColumn Header="Company ID" Binding="{Binding CompanyId}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
