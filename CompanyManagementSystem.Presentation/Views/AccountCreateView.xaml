<UserControl x:Class="CompanyManagementSystem.Presentation.Views.AccountCreateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Border Style="{StaticResource CardStyle}" 
                VerticalAlignment="Center" HorizontalAlignment="Center" 
                Width="400">
            <StackPanel>
                <TextBlock Text="New Account" Style="{StaticResource HeaderStyle}" />
                <TextBlock Text="Set up a new financial account" Style="{StaticResource SubHeaderStyle}" />

                <TextBlock Text="Account Name *" Style="{StaticResource LabelStyle}" />
                <TextBox Text="{Binding AccountName, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource ModernTxt}" Margin="0,0,0,15" />

                <TextBlock Text="Group *" Style="{StaticResource LabelStyle}" />
                <TextBox Text="{Binding Group, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource ModernTxt}" Margin="0,0,0,15" />

                <TextBlock Text="Opening Balance" Style="{StaticResource LabelStyle}" />
                <TextBox Text="{Binding Balance, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource ModernTxt}" Margin="0,0,0,25" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Cancel" Command="{Binding CancelCommand}" 
                            Style="{StaticResource SecondaryBtn}" Margin="0,0,10,0" Width="100"/>
                    <Button Content="Save Account" Command="{Binding SaveCommand}" 
                            Style="{StaticResource ModernBtn}" Width="140"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
